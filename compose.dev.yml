services:
    app:
        build:
            context: .
            args:
                - NODE_ENV=development
        volumes:
            - .:/usr/src/app
            - /usr/src/app/node_modules
        restart: always
        command: yarn start:dev
